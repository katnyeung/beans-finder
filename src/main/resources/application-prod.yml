spring:
  jpa:
    hibernate:
      ddl-auto: validate  # Don't auto-create schema in prod
    show-sql: false

coffee:
  crawler:
    enabled: true
    delay-seconds: 3  # More conservative in production

logging:
  level:
    com.coffee: INFO
    org.springframework.web: WARN
